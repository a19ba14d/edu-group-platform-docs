# 术语表

- **集团（Group）**：拥有多所学校的上级组织，具备集团视角与指定学校视角切换能力。
- **学校（School）**：租户隔离的最小单位，业务数据以 `school_id` 为边界。
- **school_id**：学校唯一标识（UUID）；用于多租户隔离。
- **X-View-Scope**：集团端切换视角的请求头（`all` 或 `{school_uuid}`）。
- **RBAC/ABAC**：基于角色/属性的访问控制模型；需叠加 school_id 与组织路径等范围条件。
- **审计日志**：对关键操作与集团代理进入学校视角行为的可追溯记录。
